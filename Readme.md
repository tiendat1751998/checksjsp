sql java sẽ trả về một đối tượng object resultset

trong trường hợp không thể đóng connection thì phải thêm finnally để close connection

prepestatment vs statment
prepestatment có thể truyền tham số trực tiếp vào câu sql
statment thì không thêm tham số trực tiếp được
// dependency injection
khi cÓ quá nhiều class c


servlet weld
quản lý  object tránh khởi tạo thừa thãi dữ liệu
ví dụ tạo 1  new service thì không cần tạo mới nhiều lần


garbage collection

dọn dẹp những bộ nhớ không sử dụng trong java

mọi contoller đều trả ra biến là model

abstract là class chung

model  sẽ là mắc xích trung gian giao tiếp giữa controler và view
trong tag lib chứa tất cả thư viện hỗ trợ cho jsp

-Authentication khi đăng nhập bằng user / password vào hệ  thống thì sẽ  gọi là Authentication  (phần quyền của bạn dựa trên vai trò của bạn trong hệ thống)
- authoriazation () nó sẽ chặn và phân quyền các vấn đề truy cập cho hệ thống  ví dụ admin1 sẽ được phân quyền cho màn chỉnh sửa nhưng không thể xóa được như admin 
